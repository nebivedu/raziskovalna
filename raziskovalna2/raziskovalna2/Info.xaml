<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="raziskovalna2.Info">

    <StackLayout Orientation="Vertical" BackgroundColor="Black" Padding="20,30,20,20" >

        <Label Text="Description" TextColor="White" FontSize="24" FontAttributes="Bold"/>
        <!--<Entry x:Name ="lol" Text="{Binding opis}" IsReadOnly="True" />-->

        <Label Text="{Binding opis}" x:Name="label_opis" VerticalOptions="Center" TextColor="White"/>


        <Label Text="{Binding opis}" x:Name="label_ocena" VerticalOptions="Center" FontSize="18" TextColor="White" FontAttributes="Italic" Padding="0,30,0,0"/>

            
        <Button
                HorizontalOptions="CenterAndExpand"
                VerticalOptions="EndAndExpand"
                Text="Home"
                WidthRequest="100"
                TextColor="White"
                Clicked="Button_Clicked_Home1"
                BackgroundColor="#24bcd4"
            
            />
        

    </StackLayout>
    
</ContentPage>